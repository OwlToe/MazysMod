tick = 0 //Increases every frame, while active. The mouse moves when tick == duration
duration = 10 //Mouse moves every 10 frames
active = true //Determines whether mouse is moving
target = noone //Instance ID of nearest oMapPoint to oMouse